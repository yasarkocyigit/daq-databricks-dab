source:
  name: adventureworks
  type: jdbc
  description: "AdventureWorks OLTP database on SQL Server"

  connection:
    driver: "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    host_secret_scope: "adls-secrets"
    host_secret_key: "sqlserver-host"
    port: 1433
    database: "AdventureWorks2019"
    username_secret_key: "sqlserver-username"
    password_secret_key: "sqlserver-password"

    jdbc_options:
      encrypt: "true"
      trustServerCertificate: "true"
      loginTimeout: 30
      queryTimeout: 7200

  # -- Streaming source configs (uncomment when needed) --
  # eventhub:
  #   namespace_secret_key: "eventhub-namespace"
  #   connection_string_secret_key: "eventhub-connection-string"
  #   consumer_group: "$Default"
  #   starting_position: "earliest"

  # kafka:
  #   bootstrap_servers_secret_key: "kafka-brokers"
  #   security_protocol: "SASL_SSL"
  #   sasl_mechanism: "PLAIN"
  #   sasl_username_secret_key: "kafka-username"
  #   sasl_password_secret_key: "kafka-password"

  # autoloader:
  #   path: "abfss://landing@daqprod.dfs.core.windows.net/incoming/"
  #   format: "json"
  #   schema_location: "abfss://datalake@daqprod.dfs.core.windows.net/_schemas/autoloader/"
